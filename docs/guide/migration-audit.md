# 文档迁移状态

本文档用于记录 Annil 文档从旧目录迁移到 `docs/` 的完成情况。

## 结论

- 迁移已完成。
- 旧目录 `doc/` 已删除。
- 对外文档主线已切换到 `CustomComponent + ChunkComponent`。

## 已完成项

- API 导航与总览已更新为当前导出主线。
- `DefineComponent` 示例已迁移到新方案。
- `wxSugar` 已覆盖 `navigateTo / navigateBack / redirectTo`。
- 示例页 `computed / watch / store` 已从旧写法迁移。
- README 文档链接已切换到 `docs/`。

## 兼容说明

- 保留了 `SubComponent（旧）` 页面用于历史项目参考。
